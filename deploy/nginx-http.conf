# Bootstrap site: issue the certificate before installing nginx.conf.
server {
    listen 80;
    listen [::]:80;
    server_name prx.azimuthglobal.co;

    location ^~ /.well-known/acme-challenge/ {
        root /var/www/letsencrypt;
    }

    location / {
        return 503;
    }
}
